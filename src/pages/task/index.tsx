import React from "react";
import { Link, useNavigate } from "react-router-dom";

const Task = () => {
  const navigateTo = useNavigate();

  return (
    <div>
      <h1>Tasks works!</h1>
    </div>
  );
};

export default Task;
